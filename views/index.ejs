<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CloneX Trends</title>
    <link rel="stylesheet" href="/css/tailwind.css">
    <link href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        :root {
            --gradient-start: #00FFFF;
            --gradient-middle: #00FF9D;
            --gradient-end: #FFB6C1;
        }
        body {
            font-family: 'Space Grotesk', sans-serif;
            background-color: #0A0F1C;
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
        }
        .glass-card {
            background: rgba(255, 255, 255, 0.05);
            backdrop-filter: blur(10px);
            border-radius: 24px;
            border: 1px solid rgba(255, 255, 255, 0.1);
        }
        .gradient-text {
            background: linear-gradient(to right, var(--gradient-start), var(--gradient-middle));
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
        }
        .feature-card {
            background: rgba(255, 255, 255, 0.03);
            backdrop-filter: blur(5px);
            border-radius: 16px;
            border: 1px solid rgba(255, 255, 255, 0.05);
            transition: all 0.3s ease;
        }
        .feature-card:hover {
            transform: translateY(-5px);
            background: rgba(255, 255, 255, 0.07);
        }
    </style>
</head>
<body>
    <div class="container mx-auto px-4 py-12">
        <div class="glass-card p-12 max-w-6xl mx-auto">
            <h1 class="text-6xl font-bold text-center mb-6 gradient-text">CloneX Trends</h1>
            <p class="text-gray-300 text-center text-xl mb-12">
                Connect your X account to transform trending images with your profile avatar.
            </p>

            <% if (!user) { %>
                <div class="flex justify-center mb-16">
                    <a href="/auth/login" class="bg-white text-black font-medium px-6 py-3 rounded-full flex items-center space-x-2 hover:bg-gray-100 transition-colors">
                        <svg class="w-5 h-5" viewBox="0 0 24 24" fill="currentColor">
                            <path d="M18.244 2.25h3.308l-7.227 8.26 8.502 11.24H16.17l-5.214-6.817L4.99 21.75H1.68l7.73-8.835L1.254 2.25H8.08l4.713 6.231zm-1.161 17.52h1.833L7.084 4.126H5.117z"/>
                        </svg>
                        <span>Connect X Account</span>
                    </a>
                </div>
            <% } else { %>
                <div class="flex justify-center mb-16">
                    <a href="/dashboard" class="bg-white text-black font-medium px-6 py-3 rounded-full hover:bg-gray-100 transition-colors">
                        Go to Dashboard
                    </a>
                </div>
            <% } %>

            <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
                <!-- Trending Images Card -->
                <div class="feature-card p-8 text-center">
                    <div class="text-4xl mb-4">🔥</div>
                    <h3 class="text-white text-xl font-semibold mb-2">Trending Images</h3>
                    <p class="text-gray-400">Browse the latest trending images from X.</p>
                </div>

                <!-- Avatar Swap Card -->
                <div class="feature-card p-8 text-center">
                    <div class="text-4xl mb-4">🔄</div>
                    <h3 class="text-white text-xl font-semibold mb-2">Avatar Swap</h3>
                    <p class="text-gray-400">Swap your profile avatar into trending images.</p>
                </div>

                <!-- Personalized Trends Card -->
                <div class="feature-card p-8 text-center">
                    <div class="text-4xl mb-4">✨</div>
                    <h3 class="text-white text-xl font-semibold mb-2">Personalized Trends</h3>
                    <p class="text-gray-400">Get personalized image recommendations.</p>
                </div>
            </div>
        </div>
    </div>
</body>
</html> 